/*const express = require('express');
const {
  listCompanies,
  listProfessionals,
  listServices,
} = require('../controllers/companiesController');

const router = express.Router();

router.get('/', listCompanies); // Listar empresas
router.get('/:id/professionals', listProfessionals); // Profissionais da empresa
router.get('/:id/services', listServices); // Serviços da empresa

module.exports = router;*/